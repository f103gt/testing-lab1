# 2,6,6 Дві неспівпадаючі впорядковані пари (x1, y1), (x2, y2), (x1 - x2)2+ (у1 - у2) 2 0, що є координатами двох
# різних точок, через які проходить одна пряма, подана рівнянням (2);
# дві неспівпадаючі впорядковані четвірки (x0i,y0i,ai,bi), ai2 +bi2 0, де (x0i,y0i) – координати точки, через яку
# проходить пряма, подана рівнянням (6), (ai,bi) – її напрямний вектор, i=1,2
# [-147;147]


# 1

# 100%
# Лабораторна робота  No  1_07_03_2026. Створення  тестовного  коду  та  його  розробницьке
# тестування
# Мета  роботи.  Оволодіти  основними  прийомами  тестопридатного  кодування  та  розробницького
# тестування коду для забезпечення його тестовності (на прикладі консольного застосунку або Windows-
# форми).
# Теоретичні відомості:
# 1)  обов’язкові  найпростіші  прийоми  створення  програмістом  стійкого  й  тестопридатного  коду
# (Авраменко_Т_ПЗ2017.pdf,  підрозділ  12.3,  с.246-251;  розд.13,  с.254-258;  розд.14,  насамперед  14.5,
# с.259-269; );
# 2)  систематизація аспектів тeстовності (або тестопридатності,  testability)  створюваного програмного
# продукту (Testability_Bach2024.pdf; Tarlinder2017.pdf, ch.4);
# 3)  систематизація  ефективних  засобів  забезпечення  програмістом  тестопридатності  створюваного
# коду  (Writing Testable Code, P.Camara, https://medium.com/feedzaitech/writing-testable-code-
# b3201d4538eb; Tarlinder2017.pdf, ch.6; за  можливості  TCode_Hevery.pdf,
# https://github.com/mhevery/guide-to-testable-code; Laboon_Fr_Int_ST_2016.pdf, ch.16);
# 4)  техніки  еквівалентного  розбиття  та  аналізу  граничних  значень,  тестування  потоку  керування
# (Л2_ДинT_02_02_2026.pdf);
# 5) рамкова стратегія модульного тестування, (Л3_Мод_Інт_Сист_Т_09_02_2026.pdf):
# 1.  Визначити  припустимі  й  неприпустимі  класи  еквівалентності  для  вхідних  і вихідних  змінних.
# тестованого коду. Спроєктувати:
# –  для  кожного  припустимого  класу  еквівалентності  – тести  на  його  границях,  для  типового
# «середнього»  значення  та  якнайближче  до  границь  у  сенсі,  відповідному  природі  вхідних  змінних
# тестованого коду;
# –  для кожного неприпустимого класу еквівалентності – тести для типового «середнього» значення та
# на  його  границях,  якщо  вони  не  співпадають  з  границями  суміжного  припустимого  класу,  або
# якнайближче до них,
# 2.  За  потреби,  спроєктувати  тести  за  допомогою  техніки  припущення  про  помилки:  для  перевірки
# відсутності  принаймні  помилок  ділення  на  0,  незадовільних  індексів  масиву,  зациклювання,
# переповнення розрядної сітки.
# 3. За потреби, на підставі аналізу тексту коду спроєктувати тести для перевірки:
# – коректності граничних значень змінних циклу;
# – коректності відпрацювання циклів, коли змінна циклу має значення всередині припустимого
# діапазону;
# – неможливості набуття змінною значень поза цим діапазоном.
# Розробницьке тестування коду програми завершують, коли, на думку програміста, вона безвідмовно
# виконується в  усіх  ситуаціях,  передбачених  специфікацією,  а  код  є  тестопридатним,  тобто  має
# характеристики, описані в Tarlinder2017.pdf, ch.4.
# Кроки виконання роботи
# 1.  Розробити  консольний  застосунок  або  Windows-форму  (на  вибір  студента),  що  має  визначати
# взаємне розміщення на площині трьох  невпорядкованих прямих, параметри подань  яких відповідають
# прізвищу студента в табл. 2, обчислювати координати точок їх перетину, за наявності, та видавати одне
# з повідомлень відповідно до вхідних даних:
# «Прямі співпадають»;
# «Прямі не перетинаються»;
# «Єдина точка перетину прямих (x0, y0), x0= ..., y0= ...»;
# «Дві точки перетину прямих (x1, y1) = ..., (x2, y2) = ...»;
# «Три точки перетину прямих (x1, y1) , (x2, y2), (x3, y3), xi= ..., yi= ..., i=1,2,3»;
# Для цього:
# 1) привести подання прямих зі свого завдання до загального вигляду (1), поданого на с.3;
# 2) визначити їх коефіцієнти Ai,  Bi,  Ci,  i=1,2,3  в отриманих поданнях як функції від вхідних змінних із
# завдання;
# 3) застосувати наведені на с.4 умови паралельності та  збігу  двох прямих або формули для точки їх
# перетину в  загальному вигляді   для отримання аналітичних виразів  щодо значень вхідних змінних із
# завдання,  які  описують  кожний  з  п’яти  наведених  випадків  розміщення  прямих  (тобто  відповідні
# припустимі класи еквівалентності) та координати точок перетину прямих для випадків 3-5.
# Ці вирази разом з іншими вимогами завдань 1,  2  застосувати як остаточну специфікацію вимог до
# створюваного застосунку та реалізувати в створюваному коді.


# 2
# Окремо  проаналізувати та  врахувати  в  коді  ситуації,  коли  певні  коефіцієнти  в  отриманих
# поданнях прямих загального вигляду дорівнюють нулю.
# Вважати:
#  параметри подань прямих, тобто значення вхідних змінних, – цілі числа з числового проміжку
# [- 100 – N; 100 + N], поданого в табл. 2; N – номер прізвища студента ІПС-41, ІПС-42 у табл.2;
#  дійсне число z дорівнює 0, якщо |z|  =10-8.
# Рішення щодо структури програми (цілісний код; головна програма й підпрограми; ієрархія класів; ...),
# парадигми й мови програмування, середовища реалізації студент має прийняти самостійно. Враховуючи
# призначення лаб. роботи та просту логіку створюваного застосунку, рекомендовано оформлювати його
# як один окремий модуль.
# Границі  числового  проміжку,  якому  мають  належати  коректні  параметри  прямих,  подати  в  коді
# застосунку як константи.
# Передбачити  введення  тестових  значень  вхідних  змінних  користувачем  з  консолі  як  відповіді  на
# необхідні  повідомлення  від  застосунку:  «Задайте  ...»  або  за  допомогою  кнопок  форми,  а  також
# виведення вихідних даних і діагностичних повідомлень на консоль або у полях форми.
# 2. У коді обов’язково реалізувати:
# а)  оброблення довільних некоректних вхідних даних і  позаштатних ситуацій під час виконання
# за  допомогою відповідних  виключень  (враховуючи   настанови  в розд.13,  14
# Авраменко_Т_ПЗ2017.pdf) і  надання  відповідних діагностичних  повідомлень  про  характер
# некоректності  та  рекомендовані  дії  з  її  усунення  у  форматі:  «опис  допущеної  помилки»;  «опис  дій  з
# виправлення помилки»;
# б)  спеціальні  випадки вхідних  даних,  коли  обчислення за  вищенаведеними  формулами  є
# надлишковими (напр., три прямі перетинаються в очевидній єдиній точці).
# У  разі  недотримання  вимог  а),  б)  та/або  явних  порушень  тестовності,  зокрема  недоліків  1)-4)  з
# TCode_Hevery.pdf, з оцінки за лаб.роботу може бути знято до 3 балів.
# 3. Відлагодити створюваний застосунок, тобто досягти успішного виконання для нього хоча б одного
# тесту кожного припустимого й неприпустимого класу, поєднуючи:
# –  доцільне  застосуванням  відлагоджувальних  операторів  друку  та/або  вбудованих  відладчиків  за
# потреби;
# –  «перевірку  за  столом»  (desk  checking)  створеного  коду,  тобто  його  уважний  перегляд  для
# виявлення  й  виправлення  типових  помилок  згідно  з  опитувальником,  поданим  в
# Авраменко_Т_ПЗ2017.pdf, підрозділ 12.3.
# 4.  Продовжити відлагодження у форматі розробницького тестування створюваного застосунку, доки
# не буде розроблено й успішно виконано тестовий набір для підтвердження, що поведінка застосунку в
# усіх ситуаціях, передбачених завданням, відповідає очікуваній.
#  Цей  тестовий  набір  має  бути  повним  у  сенсі  вищезазначеної  спрощеної  рамкової  стратегії
# модульного тестування, а саме містити:
# а)  принаймні  шість тестів  для  кожного  припустимого  класу  еквівалентності,  в  яких  значення
# параметрів подання:
# 1) усіх трьох прямих – ліва границя класу;
# 2) усіх трьох прямих – права границя класу;
# 3) усіх трьох прямих – типове значення («середина») класу;
# 4)  однієї  прямої  –  найближче  до  лівої  границі;  другої  –  типове  значення  («середина»)  класу,
# третьої – найближче до правої границі;
# 5)  двох  прямих  –  найближче  до  лівої  границі  та  наступне  за  ним,  третьої  –  типове  значення
# («середина») класу;
# 6) двох прямих – найближче до правої границі й те, що передує йому, третьої – типове значення
# («середина») класу;
# б)  принаймні  три  тести  для  кожного  неприпустимого  класу  еквівалентності,  в  яких  значення
# параметрів подання:
# 1) усіх трьох прямих – ліва границя класу;
# 2) усіх трьох прямих – права границя класу;
# 3) усіх трьох прямих – типове значення («середина») класу.
# За потреби, застосувати для проєктування тестів безкоштовний графічний калькулятор – напр.,
# Desmos, https://www.desmos.com/calculator?lang=uk
# Geogebra, https://www.geogebra.org/graphing?lang=uk


# 3
# Mathos_AI, https://www.mathgptpro.com/uk/app/graph-calculator
# або  інший  на  вибір  студента,  щоб  отримувати  відомості  щодо  очікуваного  результату  відпрацювання
# розробленого застосунку на даних потенційного тесту.
# Розробницьке  тестування  завершують,  коли,  на  думку  розробника,  поведінка  застосунку  відповідає
# очікуваній в усіх ситуаціях, передбачених специфікацією – завданням з табл. 2, а код є тестовним.
# Якщо  застосунок  являє  собою  ієрархію  модулів/класів,  намагатися  забезпечити  їх  тестовність  і
# відлагодити за допомогою службових програм для передавання тестових даних в обсязі, прийнятному
# для студента.
# Остаточне  відлагодження  й  розробницьке  тестування для  заповнення  табл.1 виконувати  для
# застосунку загалом.
# Якщо відтестувати застосунок не вдалося –  розробити тести, що описують його очікувану поведінку
# для всіх коректних і некоректних вхідних даних і задокументувати в табл.1 результати їх запускання для
# коду програми на момент здавання лаб. роботи.
# 5. За результатами проєктування й запускання тестів заповнити табл.1.
# Таблиця 1 – Результати тестування застосунку
# Тест Результат виконання
# Очікуваний Спостережений Сутність відмови Коментар (за
# потреби)
# Коректні дані
# 1
# 2
# Некоректні дані

# Для  тестів,  спостережений  результат  яких  збігається  з  очікуваним,  у  стовпці  «Спостережений»
# проставити  +.  Якщо  збіг  має  місце  для  всіх  тестів,  стовпці  «Спостережений»  і  «Сутність  відмови»
# (позначені курсивом) в табл.1 не наводити. У стовпці Коментар вказати для кожного тесту його номер –
# 1-6 для коректних даних, 1-3 для некоректних з п.4, а), б) на с.2.
# Лише  за  бажання  студента,  в  стовпці  «Коментар»  можна  подати  графічне  подання  розміщення
# прямих для відповідного тесту (успішного або ні) з використаного графічного калькулятора.
# За  наявності,  додаткові  результати  тестування  окремих модулів  подати  у  форматі  табл.1,  але  в
# заголовку вказати найменування модуля.
# Очікувані результати роботи
# 1. Аналітичні вирази,  що описують припустимі класи еквівалентності  та координати точок перетину
# для  класів  3-5 – у  форматі  текстового  файлу  (.doc,  .docx,  .pdf  на  вибір  студента)  з  іменем
# Лаба1_Формули_No групи_Прізвище. Можна написати ручкою на папері та надіслати фото.
# 2. Текст створеного коду у форматі текстового файлу (.txt,, doc, .docx, .pdf на вибір студента) з іменем
# Лаба1_Код_No групи_Прізвище.
# 3. Заповнена табл.1 та її варіанти для окремих модулів,  за наявності, у форматі файлу (.doc, .docx,
# .xls, .pdf на вибір студента) з іменем Лаба1_Тести_No групи_Прізвище.
# Результати надіслати викладачу на адресу olgaslab@knu.ua та/або olsips2017@gmail.com.
# Подання прямої, використані в завданнях лабораторної роботи.
# 1. Загальне рівняння прямої на площині:
# , A2 +B20.       (1)
# Окремі випадки рівняння прямої Ax +By +C=0:
# 1) Якщо C=0, то пряма проходить через початок координат.
# 2) Якщо A=0, то пряма паралельна осі абсцис Ox.
# 3) Якщо B=0, то пряма паралельна осі ординат Oy
# 4) Якщо A=C=0, то пряма співпадає з віссю Ox.
# 5) Якщо B=C=0, то пряма співпадає з віссю Oy.
# 2. Рівняння прямої, що проходить через задані різні точки (x1,у1), (x2,y2)
# (x - x1)/( x2- x1) = (y - y1)/( y2 - y1), (x1 - x2)2+ (у1 - у2) 2 0.     (2)
# 3. Канонічне рівняння прямої на площині


# 4
# (x - x0)/l = (y – y0)/m, l,m 0,.       (3)
# де (x0,y0.) – координати точки, через яку проходить пряма,
# (l,m) – вектор, якому пряма паралельна, званий її напрямним вектором.
# 4. Рівняння прямої у відрізках a,b  0, які вона відтинає на осі абсцис Ox і на осі ординат Oy
# x/a +y/b = 1, a,b 0         (4)
# 5. Рівняння прямої з кутовим коефіцієнтом
# y = kx +b, b 0.           (5)
# 6. Рівняння прямої, що проходить через задану точку (x0,y0)  перпендикулярно до заданого вектора
# (a,b), a2 +b2 0, званого нормальним вектором цієї прямої:
# a(x - x0) + b(y-y0) = 0, a2 +b2 0.       (6)

#  Якщо додатково A1/A2 = B1/B2 = C1/C2, прямі співпадають.


#!/usr/bin/env python3
# -*- coding: utf-8 -*-


import sys
from constants import MIN_VALUE, MAX_VALUE
from helpers import is_zero, safe_input_float
from structures import (
    Line,
    line_perpendicular_to_vector,
    are_lines_parallel,
    are_lines_coincident,
    find_intersection,
    line_from_two_points,
)
from errors import InputValidationError, GeometryError


# ==================== ГОЛОВНА ЛОГІКА ====================
def analyze_three_lines(line1: Line, line2: Line, line3: Line) -> str:
    """
    Аналізує взаємне розміщення трьох прямих на площині

    Args:
        line1, line2, line3: Три прямі для аналізу

    Returns:
        Рядок з описом результату аналізу
    """
    # Спеціальний випадок - всі три прямі ідентичні (вимога 2.б)
    # Уникаємо надлишкових обчислень, якщо всі коефіцієнти однакові
    if line1 == line2 == line3:
        return "Прямі співпадають"
    
    # Перевіряємо попарну паралельність і збіг
    parallel_12 = are_lines_parallel(line1, line2)
    parallel_13 = are_lines_parallel(line1, line3)
    parallel_23 = are_lines_parallel(line2, line3)

    coincident_12 = are_lines_coincident(line1, line2)
    coincident_13 = are_lines_coincident(line1, line3)
    coincident_23 = are_lines_coincident(line2, line3)

    # Випадок 1: Всі три прямі співпадають
    if coincident_12 and coincident_13:
        return "Прямі співпадають"

    # Випадок 2: Дві прямі співпадають, третя паралельна або перетинає
    if coincident_12:
        if parallel_13:  # Третя паралельна до перших двох
            return "Прямі не перетинаються"
        else:  # Третя перетинає перші дві в одній точці
            p = find_intersection(line1, line3)
            return (
                f"Єдина точка перетину прямих (x0, y0), x0 = {p.x:.6f}, y0 = {p.y:.6f}"
            )

    if coincident_13:
        if parallel_12:
            return "Прямі не перетинаються"
        else:
            p = find_intersection(line1, line2)
            return (
                f"Єдина точка перетину прямих (x0, y0), x0 = {p.x:.6f}, y0 = {p.y:.6f}"
            )

    if coincident_23:
        if parallel_12:
            return "Прямі не перетинаються"
        else:
            p = find_intersection(line1, line2)
            return (
                f"Єдина точка перетину прямих (x0, y0), x0 = {p.x:.6f}, y0 = {p.y:.6f}"
            )

    # Випадок 3: Прямі паралельні, але не співпадають
    if parallel_12 and parallel_13:
        return "Прямі не перетинаються"

    if parallel_12 and not parallel_13:
        # Перші дві паралельні, третя їх перетинає
        p1 = find_intersection(line1, line3)
        p2 = find_intersection(line2, line3)
        return f"Дві точки перетину прямих (x1, y1) = {p1}, (x2, y2) = {p2}"

    if parallel_13 and not parallel_12:
        # Перша і третя паралельні, друга їх перетинає
        p1 = find_intersection(line1, line2)
        p2 = find_intersection(line2, line3)
        return f"Дві точки перетину прямих (x1, y1) = {p1}, (x2, y2) = {p2}"

    if parallel_23 and not parallel_12:
        # Друга і третя паралельні, перша їх перетинає
        p1 = find_intersection(line1, line2)
        p2 = find_intersection(line1, line3)
        return f"Дві точки перетину прямих (x1, y1) = {p1}, (x2, y2) = {p2}"

    # Випадок 4 і 5: Жодна пара не паралельна
    # Знаходимо всі три точки перетину
    p12 = find_intersection(line1, line2)
    p13 = find_intersection(line1, line3)
    p23 = find_intersection(line2, line3)

    # Перевіряємо, чи всі три точки співпадають
    if (
        is_zero(p12.x - p13.x)
        and is_zero(p12.y - p13.y)
        and is_zero(p12.x - p23.x)
        and is_zero(p12.y - p23.y)
    ):
        return (
            f"Єдина точка перетину прямих (x0, y0), x0 = {p12.x:.6f}, y0 = {p12.y:.6f}"
        )

    # Три різні точки перетину
    return (
        f"Три точки перетину прямих:\n"
        f"  (x1, y1) = {p12}\n"
        f"  (x2, y2) = {p13}\n"
        f"  (x3, y3) = {p23}"
    )


def input_line1() -> Line:
    """
    Введення параметрів першої прямої (рівняння 2: через дві точки)

    Returns:
        Line: Перша пряма в загальному вигляді
    """
    print("\n" + "=" * 60)
    print("ВВЕДЕННЯ ПАРАМЕТРІВ ПЕРШОЇ ПРЯМОЇ")
    print("Рівняння (2): Пряма через дві точки (x1, y1) і (x2, y2)")
    print("=" * 60)

    x1 = safe_input_float("Задайте x1 (абсциса першої точки): ", "x1")
    y1 = safe_input_float("Задайте y1 (ордината першої точки): ", "y1")
    x2 = safe_input_float("Задайте x2 (абсциса другої точки): ", "x2")
    y2 = safe_input_float("Задайте y2 (ордината другої точки): ", "y2")

    from structures import Point
    p1 = Point(x1, y1)
    p2 = Point(x2, y2)
    return line_from_two_points(p1, p2)


def input_line_perpendicular(line_number: int) -> Line:
    """
    Введення параметрів прямої (рівняння 6: через точку перпендикулярно до вектора)

    Args:
        line_number: Номер прямої (2 або 3)

    Returns:
        Line: Пряма в загальному вигляді
    """
    print("\n" + "=" * 60)
    print(f"ВВЕДЕННЯ ПАРАМЕТРІВ {line_number}-ї ПРЯМОЇ")
    print("Рівняння (6): Пряма через точку (x0, y0) перпендикулярно до вектора (a, b)")
    print("=" * 60)

    x0 = safe_input_float(
        f"Задайте x0 (абсциса точки для прямої {line_number}): ", f"x0_{line_number}"
    )
    y0 = safe_input_float(
        f"Задайте y0 (ордината точки для прямої {line_number}): ", f"y0_{line_number}"
    )
    a = safe_input_float(
        f"Задайте a (перша компонента нормального вектора для прямої {line_number}): ",
        f"a_{line_number}",
    )
    b = safe_input_float(
        f"Задайте b (друга компонента нормального вектора для прямої {line_number}): ",
        f"b_{line_number}",
    )

    from structures import Point
    p0 = Point(x0, y0)
    return line_perpendicular_to_vector(p0, a, b)


def main():
    """
    Головна функція програми
    """
    print("=" * 60)
    print("АНАЛІЗ ВЗАЄМНОГО РОЗМІЩЕННЯ ТРЬОХ ПРЯМИХ НА ПЛОЩИНІ")
    print("Варіант 2,6,6")
    print(f"Допустимий діапазон значень: [{MIN_VALUE}; {MAX_VALUE}]")
    print("=" * 60)

    try:
        # Введення трьох прямих
        line1 = input_line1()
        line2 = input_line_perpendicular(2)
        line3 = input_line_perpendicular(3)

        # Виведення отриманих рівнянь
        print("\n" + "=" * 60)
        print("ОТРИМАНІ РІВНЯННЯ ПРЯМИХ У ЗАГАЛЬНОМУ ВИГЛЯДІ:")
        print("=" * 60)
        print(f"Пряма 1: {line1}")
        print(f"Пряма 2: {line2}")
        print(f"Пряма 3: {line3}")

        # Аналіз взаємного розміщення
        print("\n" + "=" * 60)
        print("РЕЗУЛЬТАТ АНАЛІЗУ:")
        print("=" * 60)
        result = analyze_three_lines(line1, line2, line3)
        print(result)
        print("=" * 60)

    except InputValidationError as e:
        print("\n" + "!" * 60)
        print("ПОМИЛКА ВАЛІДАЦІЇ ВХІДНИХ ДАНИХ")
        print("!" * 60)
        print(f"Опис помилки: {e.message}")
        print(f"Рекомендовані дії: {e.recommendation}")
        print("!" * 60)
        return 1

    except GeometryError as e:
        print("\n" + "!" * 60)
        print("ГЕОМЕТРИЧНА ПОМИЛКА")
        print("!" * 60)
        print(f"Опис помилки: {e.message}")
        print(f"Рекомендовані дії: {e.recommendation}")
        print("!" * 60)
        return 1

    except ZeroDivisionError:
        print("\n" + "!" * 60)
        print("КРИТИЧНА ПОМИЛКА")
        print("!" * 60)
        print("Опис помилки: Спроба ділення на нуль під час обчислень")
        print("Рекомендовані дії: Перевірте коректність введених даних")
        print("!" * 60)
        return 1

    except Exception as e:
        print("\n" + "!" * 60)
        print("НЕПЕРЕДБАЧЕНА ПОМИЛКА")
        print("!" * 60)
        print(f"Опис помилки: {type(e).__name__}: {str(e)}")
        print(
            "Рекомендовані дії: Зверніться до розробника або спробуйте інші вхідні дані"
        )
        print("!" * 60)
        return 1

    return 0


if __name__ == "__main__":
    sys.exit(main())
